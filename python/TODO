# General:
   Now:
    - make communities algorithm work from command-line
   Later:
    - Predict errors and create custom exceptions to throw in those cases

# Serialization:
   Now:
    - serialize all data from raw directory in data repo
    - create script to convert pickled precinct objects to json and test with gui.cpp
   Later (Possible after project):
    - update unit tests and add test data

# Communities:
   Now:
    - create utils file filled with geometry algorithms for useful stuff